{% extends 'base.html.twig' %}

{% block page_title %}<h2>Edit Question</h2>{% endblock %}

{% block content %}
    <div class="content-wrapper">
        {{ include('question/_form.html.twig') }}

        <form method="post" action="{{ path('question_destroy', { id: question_id } ) }}">
            <div class="row uniform 50%">
                <div class="12u$" style="margin-top: 10em;">
                    <ul class="actions">
                        <li><input type="submit" value="Delete" class="delete-button" onclick="return confirm('Really delete?')" /></li>
                    </ul>
                </div>
            </div>
        </form>
    </div>
{% endblock %}
